// Unitialized variable w

import ioPlus;

class Example3 {
	public static void main(String[] args) {
	    int[] a;
	    int c;
	    int b;
		a = new int[5];
		c = 0 + 1 + 2 + 3 + 4 + a.length + 6;
		a[0] = c;
		b = a[0];
		w = new Example3();
		w.foo(a);
	}

	public int foo(int[] a){
	    int b;
	    b = a[0];
	    ioPlus.printResult(b);
		if(b < 25) {
	        ioPlus.printHelloWorld();
	    } else {
	        ioPlus.printResult(b);
	    }
		while(b < 25){
			ioPlus.printResult(b);
			b = b + 1;
		}
	    return 0;
	}
}