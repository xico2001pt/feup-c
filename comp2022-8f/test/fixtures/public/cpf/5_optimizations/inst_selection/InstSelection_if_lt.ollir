public InstSelection_if_lt {
  .construct InstSelection_if_lt().V{
      invokespecial(this, "<init>").V ;
  }

  .method public static main(args.array.String).V {
    a.i32 :=.i32 0.i32;
    if(a.i32 <.bool 0.i32) goto else0;
    a.i32 :=.i32 1.i32;
    goto endif0;
    else0:
    a.i32 :=.i32 2.i32;

    endif0:
    ret.V;
  }

}